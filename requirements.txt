# GEDOS v0.1 — MVP dependencies
# Python 3.12+

# Telegram interface (job-queue for Copilot periodic tasks)
python-telegram-bot[job-queue]>=21.0.0

# macOS Accessibility (AX Tree)
atomacos>=3.0.0
pyobjc-framework-ApplicationServices>=10.0.0
pyobjc-framework-Cocoa>=10.0.0

# Mouse and keyboard control (atomacos requires pyautogui<0.9.42)
PyAutoGUI>=0.9,<0.9.42

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# v0.2: Memory, Web, LLM, Orchestrator
sqlalchemy>=2.0.0
playwright>=1.40.0
requests>=2.31.0
langgraph>=0.0.40
langchain-core>=0.1.0
anthropic>=0.8.0
openai>=1.0.0

# v0.6: CLI
rich>=13.0.0
psutil>=5.9.0

# v0.9.3: Scheduling
apscheduler>=3.10.0

# v0.9.4: Language detection
langdetect>=1.0.9

# v0.9.5: MCP server (requires Python 3.10+)
mcp>=1.0.0

# v0.9.6: GitHub CI webhook + self-healing
flask>=3.0.0
PyGithub>=2.0.0

# Optional: for voice input via Telegram voice messages
# pip install openai-whisper

# v0.9.8: Voice output
gTTS>=2.5.0
pydub>=0.25.1
parsedatetime>=2.6
pytz>=2024.1
tzlocal>=4.3

# Testing
pytest>=8.0.0
pytest-asyncio>=1.0.0
pytest-timeout>=2.3.1
pytest-cov>=5.0.0

# Optional: semantic memory (ChromaDB + Ollama embeddings)
# Install only if you want semantic retrieval memory support.
chromadb>=0.5.0
ollama>=0.3.0
